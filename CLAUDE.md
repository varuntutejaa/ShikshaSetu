This repo is a monorepo: `frontend/` (Next.js teacher web app) and
`backend/` (FastAPI). See [README.md](README.md) for the overview.

If you're working inside `frontend/`, that directory has its own
CLAUDE.md pulling in Next.js-specific instructions — Claude Code loads it
automatically once your working directory is there.
