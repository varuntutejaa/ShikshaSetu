# Public backend URL — safe to expose to the browser (NEXT_PUBLIC_*).
# No secrets belong in this file or anywhere under src/ — DATABASE_URL,
# Supabase database credentials, service-role keys, JWT secrets, Sarvam AI,
# LLM and LiveKit secrets live only in backend/Render environment variables.
NEXT_PUBLIC_API_URL=https://shikshasetu-backend-tq7q.onrender.com
